# Examples

Public-safe examples only. Use synthetic inputs and placeholders. Do not include API keys, private data, legal files, or copied private runtime code.

`neuromancer-bootstrap` is a host-guarded Ubuntu installer/recovery scaffold for the Intel T2 node. It refuses to mutate cloud VMs. Do not commit local evidence directories or firmware binaries.
